{
  "name": "npm-is",
  "version": "1.0.1",
  "description": "Detect the package manager client running the current script. Detects NPM and yarn, as well as any hard executable path.",
  "main": "./npm-is.js",
  "homepage": "https://github.com/zetlen/npm-is",
  "bin": "./bin/npm-is",
  "scripts": {
    "format": "prettier --write .",
    "test": "prettier -c . && node test.js",
    "preversion": "npm test"
  },
  "keywords": [
    "npm",
    "yarn",
    "lifecycle",
    "assert",
    "scripts"
  ],
  "author": "zetlen <zetlen@github.com",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^2.0.5"
  },
  "prettier": {}
}
