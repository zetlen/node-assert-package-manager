{
  "name": "assert-package-manager",
  "version": "0.1.0",
  "description": "Detect the package manager client running the current script. Detects NPM and yarn, as well as any hard executable path.",
  "main": "./assert-package-manager.js",
  "homepage": "https://github.com/zetlen/node-assert-package-manager",
  "bin": {
    "assert-package-manager": "./bin/assert-package-manager"
  },
  "scripts": {
    "format": "prettier --write",
    "test": "prettier -c && node test.js",
    "preversion": "npm test"
  },
  "keywords": [
    "npm",
    "yarn",
    "lifecycle",
    "scripts"
  ],
  "author": "zetlen <zetlen@github.com",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^2.0.5"
  },
  "prettier": {}
}